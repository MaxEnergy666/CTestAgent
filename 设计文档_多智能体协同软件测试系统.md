# 多智能体协同软件测试系统 — 设计文档

---

## 一、项目概述

### 1.1 核心思想

传统测试工具通常采用单一流水线模式：生成 → 执行 → 报告。这种模式缺乏自我反思和纠错能力，容易陷入低效的测试循环。

本项目提出**多智能体协同测试架构**：将测试过程拆分为多个具有独立职责的智能体（Agent），各智能体之间通过消息总线进行通信与协作，形成**生成 - 执行 - 评审 - 反馈**的闭环系统。核心设计理念如下：

- **职责分离**：每个智能体专注单一任务，降低复杂度
- **相互监督**：评审智能体对生成和执行结果进行质量审查，识别误判和遗漏
- **对抗博弈**：生成智能体尝试"攻破"程序，评审智能体反过来挑战测试用例的质量，形成对抗式进化
- **反馈驱动**：各智能体的输出成为其他智能体的输入，信息在系统中循环流动，持续提升测试质量

### 1.2 与单智能体的对比

| 维度 | 单智能体架构 | 多智能体协同架构（本项目） |
|------|-------------|------------------------|
| 结构 | 一个程序顺序执行所有任务 | 多个独立智能体并行/交互协作 |
| 纠错能力 | 无自我审查，错误会传播 | Reviewer Agent 审查其他智能体输出，拦截误判 |
| 测试深度 | 单一策略，容易陷入局部 | 多策略竞争 + Reviewer Agent 反馈，跳出局部最优 |
| 可扩展性 | 新增功能需修改主流程 | 新增一个 Agent 即可，不影响其他模块 |
| 创新性 | 常规自动化测试工具 | 引入多智能体协作、对抗博弈、共识机制 |

### 1.3 被测项目

主要被测项目：**cJSON**（轻量级 C 语言 JSON 解析库，约 2500 行代码）

选择理由：
- 真实开源项目，具有一定规模和实际应用场景
- 接收外部输入（JSON 字符串），天然适合模糊测试
- 历史上存在多个已知漏洞，可验证测试智能体的有效性
- 可通过 gcc 直接编译，便于插桩和覆盖率收集

辅助被测程序：排序算法库、简易计算器（用于验证系统泛用性）

### 1.4 技术栈

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| 智能体框架 | Python 3.10+ | 每个 Agent 为独立类，通过消息总线通信 |
| 消息总线 | 自研 EventBus（进程内） | 发布/订阅模式，解耦智能体间通信 |
| 编译与插桩 | GCC + gcov | 代码覆盖率收集 |
| 内存检测 | AddressSanitizer (ASan) | 检测内存越界、UAF 等 |
| 静态分析 | pycparser | 解析 C 源码提取函数签名 |
| 报告输出 | Jinja2 + HTML | 可视化测试报告 |

---

## 二、多智能体架构设计

### 2.1 智能体全景图

本系统包含 **6 个智能体**，围绕一条**消息总线（Message Bus）**进行协作：

```
                        ┌──────────────────────────┐
                        │    Coordinator Agent      │
                        │       （协调者）            │
                        │                          │
                        │  · 全局任务调度            │
                        │  · 终止条件判定            │
                        │  · 智能体生命周期管理       │
                        └─────────┬────────────────┘
                                  │ 调度指令
                 ┌────────────────┼────────────────┐
                 │                │                 │
                 ▼                ▼                 ▼
    ┌──────────────────┐ ┌──────────────┐ ┌───────────────────┐
    │  Analyzer Agent   │ │Generator Agent│ │  Executor Agent   │
    │    （分析者）      │ │  （生成者）    │ │    （执行者）      │
    │                  │ │              │ │                   │
    │ · 源码静态分析    │ │ · 等价类划分  │ │ · 编译(gcc+ASan)   │
    │ · 函数签名提取    │ │ · 边界值分析  │ │ · 运行+超时控制     │
    │ · 风险热点标注    │ │ · 组合/序列   │ │ · 崩溃/信号捕获    │
    │ · 复杂度评估     │ │ · 白盒引导    │ │ · 覆盖率收集       │
  └────────┬─────────┘ └──────┬───────┘ └─────────┬─────────┘
           │                  │                    │
    ═══════╪══════════════════╪════════════════════╪══════════
    ║      │       MESSAGE    │    BUS（消息总线）   │         ║
    ═══════╪══════════════════╪════════════════════╪══════════
           │                  │                    │
           │                  ▼                    │
           │   ┌──────────────────────┐            │
           │   │   Reviewer Agent     │            │
           │   │     （评审者）        │◄───────────┘
           │   │                      │
           │   │ · 测试用例质量审查    │
           │   │ · 执行结果交叉验证    │
           │   │ · 覆盖率缺口分析     │
           │   │ · 反馈建议生成       │
           │   └──────────┬───────────┘
           │              │
           │              ▼
           │   ┌──────────────────────┐
           └──►│   Reporter Agent     │
               │     （报告者）        │
               │                      │
               │ · 缺陷分类与去重      │
               │ · 输入最小化          │
               │ · 覆盖率可视化        │
               │ · HTML 报告生成       │
               │ · 智能体协作日志      │
               └──────────────────────┘
```

---

### 2.2 智能体命名（标准命名）

为便于课堂展示和方案替换，本版本统一采用标准英文角色命名：

| 原角色 | 标准命名 | 命名说明 |
|------|---------|---------|
| Coordinator | Coordinator Agent | 负责全局调度、阶段切换与冲突仲裁 |
| Analyzer | Analyzer Agent | 负责静态分析、风险评估与约束推断 |
| Generator | Generator Agent | 负责多策略测试用例生成与变异 |
| Executor | Executor Agent | 负责编译执行、崩溃捕获与覆盖率采集 |
| Reviewer | Reviewer Agent | 负责用例质检、结果交叉验证与反馈建议 |
| Reporter | Reporter Agent | 负责缺陷归档、可视化与报告生成 |

说明：文档后续统一使用标准命名，不使用神话别名。

---

## 三、各智能体详细设计

### 3.1 Coordinator Agent（协调者）

**角色定位**：系统的"总指挥"，不直接参与测试，负责编排其他智能体的工作流程。

**核心职责**：
1. **任务调度**：根据当前测试阶段，向各智能体发送工作指令
2. **阶段管理**：控制测试从"初始探索"到"深度挖掘"到"收尾验证"的阶段切换
3. **终止判定**：综合覆盖率增长、缺陷发现速度、时间预算等因素决定何时停止
4. **冲突仲裁**：当 Generator Agent 和 Reviewer Agent 对测试策略产生分歧时进行裁决

**状态机**：

```
    ┌─────────┐    分析完成     ┌─────────┐    覆盖率>60%    ┌─────────┐
    │  INIT   │──────────────►│ EXPLORE │───────────────►│  DEEP   │
    │ 初始化   │               │ 初始探索 │                │ 深度挖掘 │
    └─────────┘               └─────────┘                └────┬────┘
                                                              │
                                   ┌──────────┐    终止条件    │
                                   │ FINALIZE │◄──────────────┘
                                   │ 收尾验证  │
                                   └──────────┘
```

各阶段策略：

| 阶段 | 策略重心 | Generator Agent 权重 | Reviewer Agent 严格度 |
|------|---------|---------------|----------------|
| EXPLORE | 快速扩大覆盖 | 等价类 35% + 边界值 35% + 随机 20% + 错误猜测 10% | 低（允许粗放生成） |
| DEEP | 精准挖掘缺陷 | 组合/决策表 35% + 状态序列 25% + 覆盖率/路径引导 30% + 错误猜测 10% | 高（严格审查质量） |
| FINALIZE | 验证与去重 | 仅对已有崩溃输入变异 | 最高（交叉验证所有缺陷） |

**关键接口**：

```python
class CoordinatorAgent(BaseAgent):
    def __init__(self, config: dict):
        self.phase = Phase.INIT
        self.global_state = GlobalTestState()

    def on_analysis_complete(self, msg: AnalysisResult):
        """Analyzer 完成分析后，启动测试循环"""
        self.phase = Phase.EXPLORE
        self.bus.publish("cmd:generate", GenerateCommand(
            strategy_weights=EXPLORE_WEIGHTS,
            focus_functions=msg.high_risk_functions,
            batch_size=100
        ))

    def on_round_complete(self, msg: RoundSummary):
        """每轮测试完成后，决定下一步"""
        self.global_state.update(msg)
        if self.should_transition_to_deep():
            self.phase = Phase.DEEP
        if self.should_stop():
            self.phase = Phase.FINALIZE
            self.bus.publish("cmd:finalize", FinalizeCommand())

    def on_conflict(self, msg: ConflictReport):
        """处理 Generator Agent 与 Reviewer Agent 之间的分歧"""
        # 基于覆盖率数据做出仲裁
        pass
```

---

### 3.2 Analyzer Agent（分析者）

**角色定位**：系统的"侦察兵"，在测试开始前对被测代码进行深度分析，为其他智能体提供情报。

**核心职责**：
1. **函数签名提取**：解析 C 头文件，提取公开 API 的参数类型和返回类型
2. **风险热点标注**：根据代码复杂度、指针操作密度、循环嵌套深度等指标，标注高风险函数
3. **参数约束推断**：分析参数类型推断有效值域
4. **调用图构建**：分析函数调用关系，识别核心路径

**风险评分算法**：

```python
def compute_risk_score(func: FunctionInfo) -> float:
    """为每个函数计算风险分数（0~1），分数越高越应优先测试"""
    score = 0.0

    # 指针参数越多，内存错误风险越高
    ptr_ratio = func.pointer_param_count / max(func.param_count, 1)
    score += ptr_ratio * 0.3

    # 圈复杂度越高，逻辑错误风险越高
    complexity_norm = min(func.cyclomatic_complexity / 20.0, 1.0)
    score += complexity_norm * 0.3

    # 代码行数越多，缺陷概率越大
    loc_norm = min(func.lines_of_code / 200.0, 1.0)
    score += loc_norm * 0.2

    # 是否包含危险函数调用（malloc, strcpy, sprintf 等）
    if func.has_unsafe_calls:
        score += 0.2

    return min(score, 1.0)
```

**输出数据结构**：

```python
@dataclass
class AnalysisResult:
    functions: list[FunctionSignature]            # 所有可测函数
    high_risk_functions: list[str]                # 高风险函数名列表
    risk_scores: dict[str, float]                 # 函数名 → 风险分
    call_graph: dict[str, list[str]]              # 调用关系图
    param_constraints: dict[str, list[ValueRange]] # 参数约束
```

---

### 3.3 Generator Agent（生成者）

**角色定位**：系统的"攻击手"，负责不断产出能触发缺陷的测试用例。

**核心职责**：
1. 根据 Coordinator Agent 指定的策略权重，综合运用多种生成策略
2. 接收 Reviewer Agent 的反馈，调整生成方向
3. 维护种子队列，优先变异高价值种子

**生成策略（与课程知识点对齐）**：

| 策略 | 原理 | 适用阶段 |
|------|------|---------|
| EquivalencePartitioner（等价类划分） | 按输入域划分有效/无效类，优先每类至少 1 个代表值 | EXPLORE |
| BoundaryValueAnalyzer（边界值分析） | 对长度、数值、索引、空值等边界及邻域集中采样 | EXPLORE |
| DecisionTableCombiner（组合/决策表） | 对多条件逻辑构造组合，覆盖关键条件组合与约束分支 | DEEP |
| StateSequenceGenerator（序列与状态） | 基于状态机/调用序列生成事件流，覆盖关键状态转换 | DEEP |
| RandomErrorGuesser（随机+错误猜测） | 随机扰动叠加经验型易错值（null、空串、超长、特殊字符） | 全阶段 |
| WhiteBoxGuidedMutator（白盒引导） | 依据覆盖率、分支缺口与关键路径反馈进行定向变异 | DEEP |

**变异操作库（按课程思路增强）**：

```python
MUTATION_OPERATORS = [
    # 错误猜测：优先注入典型易错形态
    NullOrEmptyInject(),
    SpecialCharInject(),
    ExtremeLengthInject(),

    # 随机与边界扰动
    BitFlip(prob=0.05),          # 随机翻转比特
    ByteInsert(max_len=16),      # 插入随机字节
    ByteDelete(max_len=16),      # 删除部分字节
    ByteReplace(),               # 替换部分字节
    BoundarySplice(),            # 在边界值位置拼接
    CrossOver(corpus),           # 两个种子交叉拼接
    DictionaryInsert(tokens),    # 插入语法关键词（用于组合约束）
    ChunkDuplicate(),            # 复制一段内容
    TruncateOrExtend(),          # 截断或填充至极端长度
]
```

**接收 Reviewer Agent 反馈的自适应机制**：

```python
class GeneratorAgent(BaseAgent):
    def __init__(self):
        self.strategy_weights = {}
        self.seed_corpus = SeedCorpus()
        self.focus_areas = []

    def on_generate_command(self, cmd: GenerateCommand):
        """接收 Coordinator Agent 的生成指令"""
        self.strategy_weights = cmd.strategy_weights
        self.focus_areas = cmd.focus_functions
        test_cases = self._generate_batch(cmd.batch_size)
        self.bus.publish("data:test_cases", test_cases)

    def on_reviewer_feedback(self, feedback: ReviewFeedback):
        """接收 Reviewer Agent 的质量反馈，自适应调整"""

        if feedback.type == "COVERAGE_GAP":
            # 针对未覆盖区域，按白盒引导定向补洞
            gap_cases = self._generate_targeted(
                target_lines=feedback.uncovered_lines, count=20
            )
            self.bus.publish("data:test_cases", gap_cases)

        elif feedback.type == "LOW_DIVERSITY":
            # 增大随机/错误猜测权重，提升多样性
            self.strategy_weights["random"] += 0.2
            self._normalize_weights()

        elif feedback.type == "DECISION_TABLE_GAP":
            # 对决策表缺失组合做补全
            combo_cases = self._generate_decision_table_cases(
                conditions=feedback.missing_conditions, count=30
            )
            self.bus.publish("data:test_cases", combo_cases)

        elif feedback.type == "REDUNDANT_CASES":
            # 从种子队列中移除低价值种子
            self.seed_corpus.prune(feedback.redundant_ids)

        elif feedback.type == "PROMISING_AREA":
            # 集中力量对该区域的种子进行密集变异
            self.focus_areas = feedback.promising_functions
            intensive_cases = self._generate_intensive(count=50)
            self.bus.publish("data:test_cases", intensive_cases)
```

**课程方法落地映射**：

| 课程知识点 | Generator Agent 实现方式 |
|-----------|--------------------|
| 等价类划分（EP） | 对 JSON 字段类型/取值域划分有效类与无效类，各类抽样代表值 |
| 边界值分析（BVA） | 对长度、数值、数组索引、循环次数执行边界及邻域测试 |
| 组合测试（决策表） | 对多条件判断构造最小覆盖组合，补齐关键规则缺口 |
| 序列与值测试 | 针对状态相关 API 生成事件序列并覆盖主要状态转换 |
| 随机测试 + 错误猜测 | 注入空值、特殊字符、逆序/重复、超长输入等经验型异常 |
| 白盒路径/数据流补充 | 根据覆盖率缺口与关键分支反馈，定向生成路径/变量相关输入 |

---

### 3.4 Executor Agent（执行者）

**角色定位**：系统的"试验场"，负责编译、运行、收集结果。

**核心职责**：
1. 编译被测程序（支持 gcov 插桩 + ASan）
2. 自动生成测试驱动（harness）
3. 运行测试用例，捕获崩溃/超时/内存错误
4. 收集覆盖率数据并广播

**执行流水线**：

```
TestCase
   │
   ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ 写入临时文件  │───►│  执行 harness │───►│  收集结果     │
│ (输入数据)    │    │ (subprocess  │    │ · 返回码      │
│              │    │  + timeout)  │    │ · stdout/err  │
└──────────────┘    └──────────────┘    │ · 信号/ASan   │
                                        │ · gcov 覆盖率  │
                                        └──────┬───────┘
                                               │
                                               ▼
                                        ExecutionResult
                                        → 发布到消息总线
```

**Harness 自动生成**：

```python
class HarnessGenerator:
    def generate(self, func_sig: FunctionSignature) -> str:
        """根据函数签名自动生成 C 语言测试驱动"""
        code = f'#include "{func_sig.header_file}"\n'
        code += '#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n'
        code += self._gen_input_reader(func_sig.params)
        code += 'int main(int argc, char *argv[]) {\n'

        for param in func_sig.params:
            code += self._gen_param_setup(param)

        args_str = ", ".join(p.name for p in func_sig.params)
        if func_sig.return_type != "void":
            code += f'    {func_sig.return_type} result = {func_sig.name}({args_str});\n'
        else:
            code += f'    {func_sig.name}({args_str});\n'

        code += self._gen_cleanup(func_sig)
        code += '    return 0;\n}\n'
        return code
```

**执行结果数据结构**：

```python
@dataclass
class ExecutionResult:
    test_case_id: str
    status: str                  # PASS / CRASH / TIMEOUT / ERROR
    return_code: int
    stdout: str
    stderr: str
    execution_time: float
    signal: int | None
    asan_error: str | None
    coverage_delta: set[str]     # 新覆盖的代码行
```

---

### 3.5 Reviewer Agent（评审者）⭐ 核心创新

**角色定位**：系统的"质检官"和"战略顾问"，是多智能体架构区别于单智能体的关键角色。

**设计理念**：在人类软件开发中，代码评审（Code Review）是提升质量的重要手段。同理，在自动化测试中引入独立的"评审者"来审查测试过程，可以发现其他智能体的盲区和错误。

#### 职责 1：测试用例质量审查

在 Generator Agent 生成测试用例后、Executor Agent 执行之前，Reviewer Agent 对用例进行预筛选：

```python
def review_test_cases(self, cases: list[TestCase]) -> ReviewResult:
    approved = []
    rejected = []
    suggestions = []

    for case in cases:
        # 检查 1：去重 — 与已执行用例是否过于相似
        if self._is_duplicate(case):
            rejected.append((case, "DUPLICATE"))
            continue

        # 检查 2：有效性 — 输入是否在合理范围内
        validity = self._assess_validity(case)

        # 检查 3：预期覆盖 — 估算该用例可能触发的新路径
        estimated_new_coverage = self._estimate_coverage(case)

        # 检查 4：多样性 — 与当前批次中其他用例的差异度
        diversity_score = self._compute_diversity(case, approved)

        # 综合评分决定是否通过
        total_score = (
            validity * 0.3 +
            estimated_new_coverage * 0.4 +
            diversity_score * 0.3
        )

        if total_score >= self.threshold:
            approved.append(case)
        else:
            rejected.append((case, f"LOW_SCORE:{total_score:.2f}"))

    if len(approved) / len(cases) < 0.3:
        suggestions.append(ReviewFeedback(
            type="LOW_DIVERSITY",
            message="用例多样性不足，建议增大变异力度"
        ))

    return ReviewResult(approved, rejected, suggestions)
```

#### 职责 2：执行结果交叉验证

对 Executor Agent 报告的崩溃进行二次验证，减少误报：

```python
def verify_crashes(self, crashes: list[ExecutionResult]) -> VerifyResult:
    confirmed = []
    false_positives = []

    for crash in crashes:
        # 验证 1：重新执行 3 次，确认可复现
        reproduce_count = 0
        for _ in range(3):
            result = self._re_execute(crash.test_case)
            if result.status == "CRASH":
                reproduce_count += 1

        if reproduce_count < 2:
            false_positives.append(crash)
            continue

        # 验证 2：对崩溃输入进行微小扰动，判断边界敏感性
        sensitivity = self._test_sensitivity(crash)

        # 验证 3：调用栈指纹去重
        stack_hash = self._hash_stack_trace(crash.stderr)
        is_unique = stack_hash not in self.known_crash_hashes

        if is_unique:
            self.known_crash_hashes.add(stack_hash)
            confirmed.append(ConfirmedCrash(
                crash=crash,
                reproducibility=reproduce_count / 3,
                sensitivity=sensitivity,
                stack_hash=stack_hash
            ))

    return VerifyResult(confirmed, false_positives)
```

#### 职责 3：覆盖率缺口分析与战略建议

```python
def analyze_coverage_gaps(self, coverage_data: CoverageData) -> list[ReviewFeedback]:
    feedbacks = []

    # 找出长期未被覆盖的代码区域
    uncovered = coverage_data.get_uncovered_lines()
    stale_uncovered = [
        line for line in uncovered
        if coverage_data.rounds_since_last_progress(line) > 100
    ]
    if stale_uncovered:
        feedbacks.append(ReviewFeedback(
            type="COVERAGE_GAP",
            uncovered_lines=stale_uncovered,
            message=f"发现 {len(stale_uncovered)} 行代码长期未覆盖，建议定向生成"
        ))

    # 检测覆盖率增长是否停滞
    growth_rate = coverage_data.recent_growth_rate(last_n=200)
    if growth_rate < 0.001:
        feedbacks.append(ReviewFeedback(
            type="STAGNATION",
            message="覆盖率增长停滞，建议切换策略或增加变异多样性"
        ))

    # 识别"接近突破"的分支
    near_miss = coverage_data.find_near_miss_branches()
    if near_miss:
        feedbacks.append(ReviewFeedback(
            type="PROMISING_AREA",
            promising_functions=near_miss,
            message=f"发现 {len(near_miss)} 个接近覆盖的分支，建议集中变异"
        ))

    return feedbacks
```

---

### 3.6 Reporter Agent（报告者）

**角色定位**：系统的"记录者"和"翻译官"，将机器可读的数据转化为人可读的报告。

**核心职责**：
1. **缺陷分类与去重**：按错误类型（段错误、内存泄漏、溢出等）分类
2. **输入最小化**：对每个确认缺陷的触发输入进行 delta debugging，找到最小复现输入
3. **覆盖率可视化**：生成覆盖率热力图和增长曲线
4. **智能体协作日志**：记录各智能体间的交互过程，量化协作效率
5. **HTML 报告生成**：整合所有数据输出完整测试报告

**报告结构**：

```
测试报告
├── 1. 测试概览（总用例数 / 通过率 / 缺陷数 / 总覆盖率）
├── 2. 缺陷详情表
│   ├── 缺陷 ID / 错误类型 / 最小触发输入 / 调用栈
│   └── Reviewer Agent 验证结论
├── 3. 覆盖率分析
│   ├── 行覆盖率 / 分支覆盖率
│   ├── 覆盖率增长曲线
│   └── 未覆盖代码区域标注
├── 4. 智能体协作分析
│   ├── 各智能体消息交互统计
│   ├── Reviewer Agent 拦截的误报数量
│   ├── 反馈驱动的用例命中率
│   └── 策略切换时间点与效果
└── 5. 结论与建议
```

---

## 四、消息总线与通信协议

### 4.1 消息总线设计

所有智能体通过统一的消息总线通信，采用发布/订阅模式：

```python
class MessageBus:
    """智能体间通信的核心基础设施"""

    def __init__(self):
        self._subscribers: dict[str, list[Callable]] = defaultdict(list)
        self._message_log: list[Message] = []

    def subscribe(self, topic: str, handler: Callable):
        """智能体订阅某个主题"""
        self._subscribers[topic].append(handler)

    def publish(self, topic: str, data: Any, sender: str = ""):
        """智能体发布消息到某个主题"""
        msg = Message(topic=topic, data=data, sender=sender, timestamp=time.time())
        self._message_log.append(msg)
        for handler in self._subscribers[topic]:
            handler(msg)

    def get_interaction_stats(self) -> dict:
        """统计智能体间的交互数据（供 Reporter Agent 使用）"""
        return {
            "total_messages": len(self._message_log),
            "messages_by_topic": Counter(m.topic for m in self._message_log),
            "messages_by_sender": Counter(m.sender for m in self._message_log),
        }
```

### 4.2 消息主题与订阅关系

| 消息主题 | 发布者 | 订阅者 |
|---------|--------|--------|
| cmd:start_analysis | Coordinator Agent | Analyzer Agent |
| data:analysis_result | Analyzer Agent | Coordinator Agent, Generator Agent, Reviewer Agent |
| cmd:generate | Coordinator Agent | Generator Agent |
| data:test_cases | Generator Agent | Reviewer Agent |
| data:approved_cases | Reviewer Agent | Executor Agent |
| data:execution_results | Executor Agent | Reviewer Agent, Reporter Agent |
| data:coverage_update | Executor Agent | Generator Agent, Reviewer Agent, Coordinator Agent |
| feedback:review | Reviewer Agent | Generator Agent |
| data:verified_crashes | Reviewer Agent | Reporter Agent, Coordinator Agent |
| feedback:strategy | Reviewer Agent | Coordinator Agent |
| cmd:phase_change | Coordinator Agent | Generator Agent, Reviewer Agent |
| cmd:finalize | Coordinator Agent | Reporter Agent |
| data:report_ready | Reporter Agent | Coordinator Agent |

### 4.3 单轮协作时序

```
Coordinator Agent   Generator Agent   Reviewer Agent   Executor Agent   Reporter Agent
    │                │              │              │              │
    │ cmd:generate   │              │              │              │
    │───────────────►│              │              │              │
    │                │ data:cases   │              │              │
    │                │─────────────►│              │              │
    │                │              │ [质量审查]    │              │
    │                │              │ 过滤低质量    │              │
    │                │◄─────────────│              │              │
    │                │ feedback     │              │              │
    │                │              │ approved     │              │
    │                │              │─────────────►│              │
    │                │              │              │ [执行测试]    │
    │                │              │              │ [收集覆盖率]  │
    │                │              │◄─────────────│              │
    │                │              │ exec_results │              │
    │                │              │              │─────────────►│
    │                │              │ [交叉验证]    │              │
    │                │              │ [缺口分析]    │              │
    │◄───────────────│──────────────│──────────────│              │
    │ round_summary  │              │              │              │
    │ [判断继续/切换/终止]           │              │              │
```

---

## 五、对抗与协作机制

### 5.1 Generator vs Reviewer 的对抗博弈

Generator Agent 和 Reviewer Agent 形成一种类似 GAN（生成对抗网络）的关系：

```
    Generator（生成者）                         Reviewer（评审者）
 ┌─────────────────────┐             ┌──────────────────────┐
 │ 目标：生成能触发新    │             │ 目标：确保测试用例     │
 │ 覆盖/崩溃的测试用例  │  test_cases │ 质量高、多样性好      │
 │                     │────────────►│                      │
 │  ┌── 根据反馈调整    │  feedback   │  审查 → 生成反馈      │
 │  │   策略和参数      │◄────────────│                      │
 │  └► 增加变异力度     │             │  "用例太相似"         │
 │     切换策略         │             │  "某区域未覆盖"       │
 └─────────────────────┘             └──────────────────────┘

 博弈效果：
 · Generator Agent 被迫不断提高用例质量
 · Reviewer Agent 持续发现新的改进方向
 · 系统整体测试效率随迭代轮次上升
```

### 5.2 量化评估指标

每轮结束后，计算以下指标评估协作效率：

```python
@dataclass
class RoundMetrics:
    # 基本指标
    total_generated: int              # 本轮生成的总用例数
    reviewer_approved: int            # Reviewer Agent 通过的用例数
    approval_rate: float              # 通过率

    # 效果指标
    new_coverage_lines: int           # 新覆盖的代码行数
    new_crashes_found: int            # 新发现的崩溃数
    false_positives_caught: int       # Reviewer Agent 拦截的误报数

    # 效率指标
    coverage_per_case: float          # 每个用例带来的平均覆盖增长
    useful_case_ratio: float          # 有价值用例比例
    feedback_hit_rate: float          # Reviewer Agent 反馈后命中新覆盖的比例
```

---

## 六、项目目录结构

```
CTestAgent/
├── README.md
├── config.yaml                      # 全局配置
├── main.py                          # 系统入口
│
├── core/                            # 核心基础设施
│   ├── __init__.py
│   ├── base_agent.py                # Agent 基类
│   ├── message_bus.py               # 消息总线
│   ├── message_types.py             # 所有消息类型定义
│   └── global_state.py              # 全局测试状态
│
├── agents/                          # 六大智能体
│   ├── __init__.py
│   ├── coordinator.py               # 协调者 Agent
│   ├── analyzer.py                  # 分析者 Agent
│   ├── generator.py                 # 生成者 Agent
│   │   └── strategies/
│   │       ├── equivalence_partitioner.py
│   │       ├── boundary_value.py
│   │       ├── decision_table_combiner.py
│   │       ├── state_sequence_generator.py
│   │       ├── random_error_guesser.py
│   │       ├── whitebox_guided.py
│   │       └── mutator.py
│   ├── executor.py                  # 执行者 Agent
│   │   ├── compiler.py
│   │   ├── harness.py
│   │   ├── runner.py
│   │   └── coverage.py
│   ├── reviewer.py                  # 评审者 Agent
│   └── reporter.py                  # 报告者 Agent
│
├── targets/                         # 被测项目
│   ├── cjson/
│   ├── sort/
│   └── calculator/
│
├── seeds/                           # 初始种子
│   └── json/
│
├── output/                          # 运行时输出
│   ├── crashes/
│   ├── corpus/
│   ├── coverage/
│   ├── reports/
│   └── agent_logs/                  # 智能体交互日志
│
└── tests/                           # 项目自身测试
    ├── test_message_bus.py
    ├── test_generator.py
    ├── test_reviewer.py
    └── test_executor.py
```

---

## 七、配置文件

```yaml
# config.yaml
project:
  name: "cJSON Multi-Agent Testing"
  source_dir: "./targets/cjson"
  entry_function: "cJSON_Parse"
  compile_flags: "-Wall -g"

agents:
  coordinator:
    explore_to_deep_threshold: 0.6
    stagnation_limit: 500
    max_rounds: 200
    max_time_seconds: 1800

  generator:
    batch_size: 50
    explore_weights:
      equivalence: 0.35
      boundary: 0.35
      random: 0.20
      error_guess: 0.10
    deep_weights:
      decision_table: 0.35
      state_sequence: 0.25
      whitebox_guided: 0.30
      error_guess: 0.10

  executor:
    timeout_per_case: 5
    enable_asan: true
    enable_gcov: true

  reviewer:
    approval_threshold: 0.4
    crash_verify_count: 3
    diversity_weight: 0.3
    coverage_weight: 0.4
    validity_weight: 0.3

  reporter:
    minimize_crashes: true
    report_format: "html"

output:
  dir: "./output"
```

---

## 八、创新点总结

| 创新点 | 说明 |
|--------|------|
| 多智能体协同架构 | 6 个专职智能体通过消息总线协作，而非传统的单体流水线 |
| Generator-Reviewer 对抗博弈 | 类似 GAN 的思想，生成者与评审者相互博弈，持续提升测试用例质量 |
| Reviewer 交叉验证 | 对崩溃结果进行多次复现验证和调用栈指纹去重，大幅降低误报率 |
| 自适应策略切换 | Coordinator Agent 根据测试阶段和覆盖率增长自动切换策略权重 |
| 覆盖率缺口反馈闭环 | Reviewer Agent 分析未覆盖区域，反馈给 Generator Agent 进行定向生成 |
| 智能体协作效率分析 | Reporter Agent 记录并分析智能体间的交互数据，量化协作效果 |

---

## 九、风险与应对

| 风险 | 影响 | 应对策略 |
|------|------|---------|
| 消息总线性能瓶颈 | 智能体通信延迟 | 采用进程内同步调用，避免网络开销 |
| Reviewer Agent 过于严格 | 大量用例被拒绝，测试进度慢 | Coordinator Agent 动态调整评审阈值 |
| Reviewer Agent 过于宽松 | 冗余用例过多，执行浪费时间 | 监控 useful_case_ratio，低于阈值时提高严格度 |
| 智能体间死锁 | 系统卡住 | Coordinator Agent 设置每轮超时，超时强制推进 |
| cJSON 太稳定 | 找不到缺陷 | 准备含已知漏洞的旧版本 + 自编缺陷程序 |
| 覆盖率引导效果差 | 测试深度不足 | 人工编写高质量初始种子，提升决策表与状态序列策略占比 |


