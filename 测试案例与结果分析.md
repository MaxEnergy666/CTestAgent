# 测试案例与结果分析

> 本章节用于高校《软件测试》课程设计性实验报告，围绕 CTestAgent 多智能体协同测试系统选取三个 C 语言目标程序进行说明。本文档不重新启动测试服务，数据来自当前项目源码扫描、已有后端日志、已有 JSON 报告和课程实验中已经确认的对照数据。

## 一、测试环境与方法说明

### 1.1 测试环境

| 项目 | 配置 |
| --- | --- |
| 操作系统 | Microsoft Windows 11 专业版 10.0.22631 |
| CPU | AMD Ryzen 5 5600 6-Core Processor |
| 内存 | 15.89 GB |
| GCC | MinGW-W64 x86_64-ucrt-posix-seh 13.2.0 |
| Node.js | v24.14.1 |
| npm | 11.11.0 |
| Git 分支 | main |
| 当前 commit | 6cc5e2a |
| 项目 tag | v1.0-final、v1.1-ui-polished |

### 1.2 测试方法

本项目采用 CTestAgent 的六智能体协同流程完成 C 语言程序测试。测试流程为：

1. 前端或 API 上传 `.c` / `.h` 文件到后端 `targets/upload_*` 临时目录。
2. Analyzer 分析源码结构、函数、危险 API 与静态缺陷线索。
3. Generator 生成测试输入，Reviewer 对用例质量、覆盖率缺口和崩溃结果进行评审。
4. Executor 编译并执行测试用例，采集覆盖率和异常退出信息。
5. Reporter 汇总覆盖率、缺陷列表、智能体日志和统计数据。

本文档中的数据来源如下：

| 案例 | 数据来源 | 说明 |
| --- | --- | --- |
| tiny_calc.c | `CTestAgent/output/case_studies/case_1_tiny_calc.log`、`run_20260430_202950/report_20260430_202950.json` | 已完成 5 分钟 API 测试，日志中有每分钟采样。 |
| vuln_calc.c | 已确认的 Plan A 对照实验数据、`run_20260430_200410/report_20260430_200410.json`、历史 agent log | 使用课程实验中已经确认的数据：覆盖率 45.38%，动态缺陷数 2，feedback_emit 31。 |
| tinyexpr.c | `CTestAgent/targets/tinyexpr/tinyexpr.c`、`tinyexpr.h` 源码扫描 | 本次未继续运行后端，仅完成源码规模、入口函数和测试过程设计整理。 |

## 二、测试案例 1：tiny_calc.c

### 2.1 被测程序简介

`tiny_calc.c` 是一个简单可控的整数计算器程序，用于验证 CTestAgent 对小规模 C 程序的源码分析、输入生成和缺陷识别能力。程序通过 `stdin` 读取一行输入，格式为：

```text
数字 运算符 数字
```

例如：

```text
10 + 5
```

程序实现加、减、乘、除四类运算，并故意保留两个缺陷点：

| 缺陷 | 位置 | 说明 |
| --- | --- | --- |
| BUG-1 | `return left / right;` | 除法前未检查除数是否为 0，输入 `10 / 0` 可触发除零风险。 |
| BUG-2 | `return left + right;` | 使用 `int` 加法且不检查溢出，大整数相加存在溢出风险。 |

源码规模：

| 指标 | 数值 |
| --- | ---: |
| 本地源码总行数 | 44 |
| 本地非空行数 | 44 |
| 测试报告覆盖率统计总行数 | 55 |
| 覆盖率统计分支数 | 14 |

完整源码如下：

```c
#include <stdio.h>

static int calculate(int left, char op, int right, int *ok) {
    *ok = 1;

    if (op == '+') {
        // BUG-2: signed integer overflow is not checked.
        return left + right;
    }

    if (op == '-') {
        return left - right;
    }

    if (op == '*') {
        return left * right;
    }

    if (op == '/') {
        // BUG-1: division by zero is intentionally not checked.
        return left / right;
    }

    *ok = 0;
    return 0;
}

int main(void) {
    char line[128];
    char op;
    char extra;
    int left;
    int right;
    int ok;
    int result;

    if (fgets(line, sizeof(line), stdin) == NULL) {
        printf("ERROR: empty input\n");
        return 1;
    }

    if (sscanf(line, "%d %c %d %c", &left, &op, &right, &extra) != 3) {
        printf("ERROR: expected format: number operator number\n");
        return 1;
    }

    result = calculate(left, op, right, &ok);
    if (!ok) {
        printf("ERROR: unsupported operator '%c'\n", op);
        return 1;
    }

    printf("%d\n", result);
    return 0;
}
```

### 2.2 测试过程

测试通过后端 API 完成，过程如下：

```bash
POST /api/upload
POST /api/start
GET  /api/status
POST /api/stop
GET  /api/report
```

测试启动时间为 `2026-04-30T20:24:45+08:00`，停止请求时间为 `2026-04-30T20:29:45+08:00`，运行时间为 300 秒。

每分钟采样数据如下：

| 时间 | Round | 行覆盖率 | 分支覆盖率 | 缺陷数 | totalCases |
| --- | ---: | ---: | ---: | ---: | ---: |
| 0 分钟 | 0 | 0.00% | 0.00% | 0 | 0 |
| 1 分钟 | 14 | 34.55% | 64.29% | 0 | 296 |
| 2 分钟 | 29 | 34.55% | 64.29% | 0 | 553 |
| 3 分钟 | 42 | 34.55% | 64.29% | 0 | 817 |
| 4 分钟 | 56 | 34.55% | 64.29% | 0 | 1041 |
| 5 分钟 | 71 | 34.55% | 64.29% | 0 | 1278 |

关键事件：

| 时间点 | 事件 |
| --- | --- |
| 0 分钟 | 测试任务启动，进入 `EXPLORE` 阶段。 |
| 1 分钟 | 覆盖率达到 34.55% 行覆盖率和 64.29% 分支覆盖率。 |
| 1-5 分钟 | 用例数量持续增长，但覆盖率不再提升。 |
| 5 分钟 | 手动停止测试，Reporter 生成报告。 |

### 2.3 测试结果

最终结果：

| 指标 | 数值 |
| --- | ---: |
| 最终状态 | finished |
| 完成轮次 | 71 |
| 总生成用例数 | 1300 |
| 总执行用例数 | 1300 |
| 行覆盖率 | 34.55% |
| 分支覆盖率 | 64.29% |
| 行覆盖数 / 总行数 | 19 / 55 |
| 分支覆盖数 / 总分支数 | 9 / 14 |
| 动态确认缺陷数 | 0 |
| 静态发现数 | 1 |
| feedback:review 消息数 | 93 |
| 消息总数 | 426 |
| 停止原因 | manual_stop |

缺陷列表：

| 类型 | 严重级别 | 位置 | 触发输入 | 说明 |
| --- | --- | --- | --- | --- |
| UNGUARDED_DIVISION | MEDIUM | `tiny_calc.c:21` | 静态分析项 | Analyzer / Reporter 发现除法表达式缺少除数为 0 的显式检查。 |

智能体活跃度：

| Agent | 消息数 |
| --- | ---: |
| Coordinator | 79 |
| Analyzer | 1 |
| Generator | 85 |
| Reviewer | 174 |
| Executor | 87 |

系统稳定性：

| 项目 | 结果 |
| --- | --- |
| 是否卡死 | 否 |
| 是否后端异常 | 否 |
| 是否生成报告 | 是 |
| 是否发现故意植入缺陷 | 是，静态发现除零风险 |

### 2.4 结果分析

`tiny_calc.c` 的测试结果符合小规模可控样例的预期。由于程序结构简单，Generator 在第一分钟内就触发了主要可达分支，覆盖率迅速达到 34.55% 行覆盖率和 64.29% 分支覆盖率。后续四分钟内用例数从 296 增加到 1278，但覆盖率没有继续提升，说明该程序的剩余未覆盖区域更可能与 harness 包装、错误路径统计或覆盖率分母有关，而不是单纯缺少随机输入。

缺陷发现方面，系统成功识别了 `return left / right;` 处的除零风险。该缺陷在报告中归类为静态发现，动态崩溃计数为 0。这个结果说明 Analyzer 与 Reporter 能够在没有实际触发崩溃的情况下发现明显危险表达式，但 Executor 在 5 分钟内没有形成被确认为崩溃的除零输入。对教学样例而言，这一结果可以体现“静态识别”和“动态触发”之间的差异。

Reviewer 在本案例中产生 93 次 `feedback:review` 消息，是最活跃的智能体。这说明即便目标程序很小，系统仍会不断根据覆盖率缺口和用例质量进行反馈。覆盖率在第一分钟后不再增长，表明后续反馈主要维持测试探索而没有打开新的代码区域。

## 三、测试案例 2：vuln_calc.c

### 3.1 被测程序简介

`vuln_calc.c` 是一个中等复杂度的教学样本，形式上是字符串计算器，内部故意植入多类 C 语言安全缺陷。该样本用于验证 CTestAgent 对内存安全问题、异常退出路径和危险 API 的识别能力。

正式目标路径：

```text
CTestAgent/targets/vuln_calc/vuln_calc.c
```

程序中标注的缺陷包括：

| 编号 | 类型 | 代码位置或函数 |
| --- | --- | --- |
| BUG-1 | 栈缓冲区溢出 | `gets(input)`、`strcpy(input, argv[1])` |
| BUG-2 | 空指针解引用 | `parse_number` 返回 NULL 后未检查 |
| BUG-3 | 整数溢出 | `calculate` 中加法和乘法 |
| BUG-4 | 除零错误 | `calculate` 中除法 |
| BUG-5 | 数组越界 | `history_count` 无上限检查 |
| BUG-6 | 内存泄漏 | `strdup` 后未释放 |
| BUG-7 | Use-After-Free / Double Free | `free(a)` 后再次 `free(a)` 并读取 |
| BUG-8 | 格式化字符串漏洞 | `printf(history[i].expression)` |

测试报告中的覆盖率统计总行数为 119，覆盖率核心验收指标采用课程实验中已经确认的 Plan A 数据。

### 3.2 测试过程

本案例使用已有 Plan A 多智能体协作实验数据，测试过程为：

1. 上传 `vuln_calc.c` 到后端临时目标目录。
2. Analyzer 识别危险 API、内存释放路径和字符串处理风险。
3. Generator 生成表达式、异常字符串、边界值和畸形输入。
4. Executor 编译并执行输入，记录异常退出码和覆盖率。
5. Reviewer 对崩溃进行签名聚合和误报过滤。
6. Reporter 生成缺陷列表、覆盖率和智能体交互统计。

关键过程数据如下：

| 指标 | 数值 |
| --- | ---: |
| 完成轮次 | 70 |
| 总生成用例数 | 1199 |
| 总执行用例数 | 1199 |
| 动态异常路径样例数 | 774 |
| 动态确认缺陷数 | 2 |
| 静态高危线索 | 3 |
| feedback_emit | 31 |
| 消息总数 | 945 |

说明：历史 JSON 报告中 `feedback:review` 主题消息数为 286；课程对照实验中用于比较 Reviewer 跨轮反馈机制的核心指标为 `feedback_emit=31`。二者统计口径不同，本文横向对比采用 `feedback_emit`。

### 3.3 测试结果

最终核心指标：

| 指标 | 结果 |
| --- | ---: |
| 行覆盖率 | 45.38% |
| 分支覆盖率 | 39.39% |
| 缺陷发现数 | 2 |
| feedback_emit | 31 |
| 覆盖行数 / 统计总行数 | 54 / 119 |
| 覆盖分支数 / 统计总分支数 | 13 / 33 |

动态确认缺陷：

| 类型 | 严重级别 | 函数 | 触发输入摘要 | 说明 |
| --- | --- | --- | --- | --- |
| nonzero_rc_3221226356 | MEDIUM | `process_input` | `[1, 2, 3,]`、`{key: 1}` 等 | 异常退出码 `0xC0000374`，表现为堆损坏类错误。 |
| nonzero_rc_136 | MEDIUM | `process_input` | 长随机畸形字符串 | 异常退出码 136，表现为 SIGABRT 或分配器中止。 |

静态高危线索：

| 类型 | 位置 | 说明 |
| --- | --- | --- |
| DOUBLE_FREE_PATTERN | `vuln_calc.c:93` | 发现重复释放 `a` 的代码路径。 |
| UNBOUNDED_INPUT | `vuln_calc.c:109` | 使用 `gets` 读取输入且没有长度限制。 |
| UNSAFE_COPY | `vuln_calc.c:105` | 使用 `strcpy` 复制命令行参数，缺少边界检查。 |

智能体活跃度：

| Agent | 消息数 |
| --- | ---: |
| Coordinator | 165 |
| Analyzer | 1 |
| Generator | 166 |
| Reviewer | 456 |
| Executor | 157 |

### 3.4 结果分析

`vuln_calc.c` 的结果符合教学漏洞样本的预期。与 `tiny_calc.c` 相比，该程序包含更多异常路径、危险库函数和内存释放错误，因此动态测试能够触发多个异常退出路径。最终动态确认缺陷数为 2，同时静态分析还报告了 3 个高危线索，覆盖了 double free、无界输入和不安全复制三类典型 C 语言缺陷。

覆盖率达到 45.38%，说明多智能体协作能持续推进到 `process_input`、`calculate` 和历史记录相关逻辑，但没有完全覆盖所有状态组合。对于这类存在大量崩溃输入的程序，Executor 频繁遇到异常退出，Reviewer 需要承担更重的崩溃聚合和误报过滤任务。这一点也反映在消息统计中：Reviewer 消息数为 456，是最活跃的智能体。

`feedback_emit=31` 说明 Plan A 的跨轮反馈机制确实参与了测试过程。与单体探索基线相比，多智能体协作没有牺牲最终覆盖率和缺陷数，同时显著提升了反馈进入生成流程的次数。这一结果支撑了系统设计中的“Reviewer 不只做事后报告，而是参与下一轮生成策略调整”的核心思想。

## 四、测试案例 3：tinyexpr.c

### 4.1 被测程序简介

`tinyexpr` 是一个真实开源 C 表达式解析与求值库，项目中已包含：

```text
CTestAgent/targets/tinyexpr/tinyexpr.c
CTestAgent/targets/tinyexpr/tinyexpr.h
```

源码规模：

| 文件 | 字节数 | 行数 | 非空行数 |
| --- | ---: | ---: | ---: |
| `tinyexpr.c` | 22912 | 601 | 600 |
| `tinyexpr.h` | 2333 | 65 | 65 |
| 合计 | 25245 | 666 | 665 |

`tinyexpr.h` 导出的主要函数如下：

```c
double te_interp(const char *expression, int *error);
te_expr *te_compile(const char *expression, const te_variable *variables, int var_count, int *error);
double te_eval(const te_expr *n);
void te_print(const te_expr *n);
void te_free(te_expr *n);
```

其中 `te_interp` 是最适合作为测试入口的函数，因为它可以直接接收字符串表达式并返回求值结果；`te_compile`、`te_eval` 和 `te_free` 更适合构造深度测试场景，用于验证解析树生命周期管理。

### 4.2 测试过程

`tinyexpr` 属于库函数型目标，不是可以直接从命令行读取输入的独立程序。因此本案例采用源码级静态测试与接口级输入设计，测试重点从“命令行参数”转为“表达式字符串和 API 调用序列”。准备过程如下：

1. 确认 `tinyexpr.c` 和 `tinyexpr.h` 均存在，且两个文件均在 Git 跟踪中。
2. 扫描 `tinyexpr.h` 导出函数，确定 `te_interp` 是最直接的字符串表达式入口。
3. 扫描 `tinyexpr.c` 中的内存分配、释放、递归下降解析、数学运算和错误返回路径。
4. 基于源码路径构造接口级测试输入，包括合法表达式、非法表达式、极端嵌套表达式、空指针调用和特殊浮点运算。

测试输入设计如下：

| 类别 | 输入或调用方式 | 目标路径 |
| --- | --- | --- |
| 基础算术 | `1+2*3` | 覆盖 `add`、`mul`、优先级解析和 `te_eval`。 |
| 括号嵌套 | `((1+2)*(3+4))` | 覆盖 `base -> list -> expr` 的递归下降路径。 |
| 函数调用 | `sin(0)+cos(0)` | 覆盖内置函数查找、函数参数解析和纯函数优化。 |
| 非法表达式 | `1+*2`、`sin(` | 覆盖 `TOK_ERROR`、错误位置返回和 `te_compile` 失败路径。 |
| 除零表达式 | `1/0`、`0/0` | 覆盖 `divide` 运算与 IEEE 浮点异常值返回。 |
| 深度嵌套 | 5000 层以上连续括号 | 检查递归下降解析的栈深度边界。 |
| 空指针调用 | `te_interp(NULL, &error)` | 检查公开 API 对非法调用参数的防御能力。 |

### 4.3 测试结果

源码级静态测试发现 `tinyexpr` 的主要风险并非传统缓冲区溢出，而集中在 API 参数防御、递归深度和浮点特殊值处理上。结果如下：

| 编号 | 缺陷或风险类型 | 位置 | 触发方式 | 严重级别 | 结果说明 |
| --- | --- | --- | --- | --- | --- |
| TE-1 | 空指针输入缺少保护 | `te_compile` / `next_token` | `te_interp(NULL, &error)` | HIGH | `te_compile` 将 `expression` 直接赋给 `s.next`，`next_token` 进入后会解引用 `s->next`。公开 API 未检查 NULL，存在空指针解引用风险。 |
| TE-2 | 深度递归导致栈耗尽 | `base`、`list`、`expr`、`power` | 极深括号或指数表达式 | MEDIUM | 解析器采用递归下降结构，嵌套表达式过深时可能耗尽调用栈，表现为异常退出或解析失败。 |
| TE-3 | 除零与特殊浮点值传播 | `divide`、`te_eval` | `1/0`、`0/0` | LOW | `divide` 直接返回 `a / b`，不会崩溃，但会产生 `inf` 或 `nan`，上层调用者若未处理特殊值，可能造成业务逻辑错误。 |
| TE-4 | 非法表达式错误位置依赖解析状态 | `next_token`、`te_compile` | `1+*2`、`sin(` | LOW | 程序能返回错误位置，但错误位置依赖当前 token 推进状态；对复杂非法输入，错误定位可能不够直观。 |

综合结果：

| 指标 | 结果 |
| --- | --- |
| 源码是否存在 | 是 |
| 头文件是否存在 | 是 |
| 是否为真实开源 C 项目 | 是 |
| 可测试入口 | `te_interp`、`te_compile`、`te_eval` |
| 静态风险数 | 4 |
| 高风险项 | 1 |
| 中风险项 | 1 |
| 低风险项 | 2 |

### 4.4 结果分析

`tinyexpr` 与前两个教学样例不同，它不是独立命令行程序，而是表达式解析库。它的风险重点不是 `gets`、`strcpy` 这类典型危险 API，而是公开 API 在异常参数、深度递归和数学特殊值场景下的鲁棒性。

从源码结构看，`te_interp` 会调用 `te_compile`，`te_compile` 内部初始化解析状态后立即进入 `next_token`。当调用者传入 NULL 表达式指针时，`next_token` 中的 `if (!*s->next)` 会对 NULL 指针解引用，因此 TE-1 是本案例最需要优先修复的问题。修复方式是在 `te_compile` 入口处增加 `expression == NULL` 判断，并通过 `error` 返回明确错误码。

TE-2 体现了递归下降解析器常见的能力边界。对于正常长度表达式，递归结构清晰且易维护；但对于恶意构造的深度括号输入，递归层数会随着表达式深度线性增长，最终可能触发栈耗尽。因此在真实系统中应限制表达式最大长度或最大括号嵌套深度。

TE-3 不一定属于 C 层面的崩溃缺陷，因为双精度浮点除零通常会返回 `inf` 或 `nan`。但从软件测试角度看，如果上层业务把该返回值继续用于金额、评分、坐标或阈值判断，就可能形成逻辑缺陷。因此该项更适合归类为低风险鲁棒性问题。

总体来看，`tinyexpr` 更适合验证 CTestAgent 从“教学漏洞程序”迁移到“真实开源库”的能力。该案例说明，真实库的缺陷不一定表现为明显崩溃，更多时候体现为边界输入、API 契约和错误处理策略是否充分。

## 五、横向对比分析

### 5.1 三个案例核心指标对比表

| 指标 | tiny_calc | vuln_calc | tinyexpr |
| --- | ---: | ---: | ---: |
| 程序类型 | 简单计算器 | 教学漏洞样本 | 开源表达式解析库 |
| 本地源码行数 | 44 | 97 | 666 |
| 覆盖率统计总行数 | 55 | 119 | 静态测试不统计 |
| 最终行覆盖率 | 34.55% | 45.38% | 静态测试不统计 |
| 最终分支覆盖率 | 64.29% | 39.39% | 静态测试不统计 |
| 完成轮次 | 71 | 70 | 静态测试不统计 |
| 总执行用例数 | 1300 | 1199 | 接口输入设计 7 类 |
| feedback_emit / feedback:review | 93 | 31 | 静态测试不统计 |
| 动态缺陷发现数 | 0 | 2 | 静态测试不统计 |
| 静态缺陷线索 | 1 | 3 | 4 |
| 是否生成报告 | 是 | 是 | 生成源码级分析结论 |

说明：`tiny_calc` 的 93 来自报告中的 `feedback:review` 主题消息数；`vuln_calc` 的 31 来自课程对照实验中用于评价 Plan A 跨轮反馈机制的 `feedback_emit` 指标。二者统计口径不同，表中保留原始口径以便追溯。

### 5.2 系统在不同复杂度目标上的表现规律

从已有数据可以观察到以下规律：

1. 对简单程序，系统能快速达到稳定覆盖率。`tiny_calc` 在第 1 分钟达到 34.55% 行覆盖率和 64.29% 分支覆盖率，随后覆盖率没有继续增长。
2. 对缺陷密集程序，系统更容易触发动态异常路径。`vuln_calc` 运行中确认 2 个动态缺陷，并识别 3 个静态高危线索。
3. Reviewer 在两个已运行案例中都是最活跃的智能体。`tiny_calc` 中 Reviewer 发送 174 条消息，`vuln_calc` 中 Reviewer 发送 456 条消息，说明评审与反馈机制在测试闭环中占据核心位置。
4. 静态发现与动态确认需要分开解释。`tiny_calc` 的除零风险被静态识别，但动态 confirmed_bugs 为 0；`vuln_calc` 同时具备动态崩溃证据和静态危险 API 证据。

### 5.3 系统的能力边界

CTestAgent 对以下目标效果较好：

- 具有明确输入入口的 C 程序，例如通过 `stdin` 或命令行参数读取输入。
- 包含危险 API、未检查除法、内存释放错误等典型 C 缺陷的教学样本。
- 能够通过随机输入、边界值和畸形字符串触发异常路径的程序。

系统在以下场景中存在限制：

- 对库函数型项目，需要 harness 将输入映射到 API 调用，否则无法直接测试。
- 覆盖率结果受 harness 和 gcov 统计口径影响，源码行数与报告分母可能不完全一致。
- 动态缺陷确认依赖执行路径，静态发现的风险不一定在有限时间内被触发为崩溃。
- 大量崩溃输入会增加 Reviewer 的聚合和误报过滤压力。

## 六、结论与改进方向

三个案例共同说明，CTestAgent 已具备“源码分析 -> 用例生成 -> 编译执行 -> 缺陷评审 -> 报告输出”的完整闭环。

`tiny_calc` 验证了系统对小规模可控程序的快速覆盖能力和静态风险识别能力；`vuln_calc` 验证了系统对多缺陷 C 程序的动态异常触发能力和 Reviewer 反馈机制；`tinyexpr` 则提供了面向真实开源库的扩展目标，说明后续需要进一步增强 harness 生成和库函数入口识别能力。

后续改进方向包括：

1. 为库函数项目自动生成更稳定的 harness，优先支持 `te_interp` 这类字符串入口。
2. 在报告中区分动态 confirmed bugs、静态 findings 和消息主题统计，避免不同口径混淆。
3. 增强除零、整数溢出等简单缺陷的动态触发策略，使静态发现能够转化为可复现输入。
4. 针对大量崩溃样本优化 Reviewer 的签名聚合能力，减少重复异常对统计结果的干扰。
5. 在前端报告中加入“数据来源”字段，明确覆盖率、缺陷数和反馈次数来自运行状态、JSON 报告还是对照实验日志。
