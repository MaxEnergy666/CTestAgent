# CTestAgent 三分钟演示解说稿

[0:00-0:25 画面停留在项目首页或系统标题]

面向 C 语言的自动化模糊测试与缺陷发现测试所开发的测试智能体，其基本思路/技术路线是通过 6 个职责分离的智能体协同工作，形成“生成、执行、评审、反馈”闭环；其特色/优势在于跨轮异步反馈调度，避免同步通信递归问题，并让 Generator 和 Reviewer 形成对抗博弈。生成器出题，评审者看覆盖率、挑毛病，再把意见带回下一轮。

[0:25-0:40 此时拖拽 vuln_calc.c 到左侧上传区]

现在把 `vuln_calc.c` 拖到上传区。这个 C 程序植入了缓冲区溢出、整数溢出、除零、double free 等缺陷。文件自动同步到后端，出现绿色“已同步到后端”提示后，就可以启动测试。

[0:40-1:50 点击启动按钮，画面切到中央主视图和底部面板]

点击启动后，中央出现 6 个智能体节点。动画不是装饰，每条连线都对应真实通信：Analyzer 分析，Generator 生成，Reviewer 审查，Executor 运行，Reporter 汇总。中心覆盖率实时变化，可看出测试是否推进。底部面板记录用例、崩溃和缺陷；曲线停滞时，Reviewer 会触发反馈，让 Generator 换策略。日志可筛选，右侧还能调权重、超时和 Reviewer 阈值。它更像可干预的测试控制台。

[1:50-2:20 画面切到代码仓库目录或 GitHub 页面]

后端职责也很清楚：`agents/` 是 6 个智能体，`core/` 是通信通道，`targets/` 放被测程序，`output/` 存日志报告，`server.py` 是 FastAPI 入口。GitHub：https://github.com/MaxEnergy666/CTestAgent，版本 tag 也保留着。

[2:20-2:40 画面展示覆盖率、缺陷列表和日志统计]

在 `vuln_calc.c` 上运行后，行覆盖率达到 45.38%，发现 2 个缺陷，Reviewer 反馈触发 31 次。这说明 Reviewer 不是摆设：它会在停滞时介入，把覆盖率转成后续策略。

[2:40-3:00 画面回到系统整体界面]

最后说边界。Reviewer 还不能精确指导 Generator 覆盖某一行；高频反馈下通信也有瓶颈；系统还缺少白盒路径求解。但这些限制指出了真实挑战。后续引入 LLM 或路径分析，就能提升定向突破能力。
