# CTestAgent 多智能体协同软件测试系统设计文档

> 本文档基于旧版 `设计文档_多智能体协同软件测试系统.md` 修订，并与当前 master 分支代码对齐。旧文档中的系统思想、六智能体划分、Reviewer 核心角色和消息总线结构被保留；与当前实现不一致的内容已经更新，例如静态分析采用正则与启发式扫描而非 `pycparser`，报告生成采用代码内 HTML / JSON 渲染而非 `Jinja2`，Reviewer 崩溃确认以签名聚合和严重度评估为主，前端支持用户上传通用 C 源文件。

---

## 一、项目概述

### 1.1 核心思想：多智能体协同测试

传统自动化测试工具通常采用单体流水线结构：生成测试输入、执行程序、收集结果、输出报告。该结构实现简单，但测试过程缺乏“自我审查”和“反馈修正”能力：生成器可能持续产出重复或低价值输入，执行器只能机械运行，报告器只能在末尾总结结果。

CTestAgent 的核心思想是将 C 语言自动化测试过程拆解为多个职责明确的智能体，由消息总线组织协作，形成持续反馈闭环：

- Analyzer 先读取被测源码，提取函数、风险点、约束和静态漏洞线索。
- Generator 根据当前阶段和策略权重生成测试用例。
- Reviewer 在执行前审查用例质量，在执行后确认崩溃并分析覆盖率缺口。
- Executor 负责编译、运行、覆盖率采集和异常捕获。
- Coordinator 统一调度轮次、阶段、终止条件和跨轮反馈。
- Reporter 将执行数据、缺陷和智能体交互汇总为报告。

这种结构把软件测试过程从“一条固定流水线”转变为“多个角色互相制约、互相补充的协作系统”。其中 Reviewer 是关键创新点：它既像代码评审者一样过滤低质量输入，又像测试策略顾问一样把覆盖率缺口反馈给 Generator。

### 1.2 与单智能体架构的对比

| 对比维度 | 单智能体 / 单体流水线 | CTestAgent 多智能体协作 |
| --- | --- | --- |
| 结构 | 单个主流程顺序执行所有任务。 | 6 个 Agent 通过消息总线发布 / 订阅协作。 |
| 职责边界 | 生成、执行、分析、报告耦合在一起。 | 每个 Agent 负责一个清晰角色，便于替换和调试。 |
| 反馈能力 | 执行结果通常只进入最终报告。 | Reviewer 将覆盖率缺口、低多样性等反馈给 Generator。 |
| 纠错能力 | 低质量输入和重复崩溃容易直接进入统计结果。 | Reviewer 做预筛选、崩溃签名聚合和风险评估。 |
| 可观测性 | 只能看到最终结果。 | MessageBus 记录消息日志，前端可实时展示协作过程。 |
| 工程风险 | 结构简单，但扩展复杂测试策略时易膨胀。 | 结构更复杂，但模块边界清晰，适合课程设计展示。 |

### 1.3 被测项目说明

当前系统不是只面向某一个固定 C 程序，而是以“通用 C 文件上传测试”为主要使用场景。用户在前端上传 `.c` / `.h` 文件后，后端会保存到 `CTestAgent/targets/upload_时间戳/` 目录，并将该目录作为本次测试目标。

仓库中同时保留了多个示例目标，用于演示和回归验证：

| 目标目录 | 说明 |
| --- | --- |
| `CTestAgent/targets/Damn Vulnerable C Program/` | 当前 `config.yaml` 默认目标，入口函数为 `ProcessImage`。 |
| `CTestAgent/targets/cjson/` | JSON 解析库样例，用于验证字符串解析类输入。 |
| `CTestAgent/targets/tinyexpr/` | 表达式解析样例，用于验证文本表达式类输入。 |
| `CTestAgent/targets/fuzzgoat/` | 带已知漏洞的 C 程序样例，用于验证崩溃发现与聚合。 |
| `CTestAgent/targets/calculator/`、`sort/` | 小型教学样例。 |
| `CTestAgent/targets/upload_*/` | 前端上传文件后自动生成的测试目录。 |

最终对照实验使用用户给定的实验数据：单体探索基线与 Plan A 多智能体协作均达到 45.38% 最终覆盖率，均发现 2 个缺陷；Plan A 的 feedback_emit 从 2 次提升到 31 次。

### 1.4 技术栈

| 层次 | 技术 | 当前实现说明 |
| --- | --- | --- |
| 后端语言 | Python 3.10+ | Agent、消息总线、运行时控制、报告生成均由 Python 实现。 |
| Web 服务 | FastAPI | `CTestAgent/server.py` 提供上传、启动、状态、报告和 WebSocket 接口。 |
| 前端框架 | Vue 3 + Vite | `ctestagent-ui/src/App.vue` 组织 Dashboard 主界面。 |
| UI 与图表 | Element Plus、ECharts、vue-echarts | 实现上传、参数配置、覆盖率报告、缺陷列表和统计图表。 |
| 编译执行 | GCC / MinGW GCC | Executor 调用 `gcc` 编译被测 C 文件与 harness。 |
| 覆盖率 | gcov | 通过 `.gcda`、`.gcno`、`.gcov` 文件统计行 / 分支覆盖率。 |
| 异常检测 | 返回码、信号、stderr、可选 ASan | Windows 默认关闭 ASan，Linux / macOS 可启用。 |
| 配置文件 | YAML | `config.yaml` 配置目标项目、Agent 参数和输出目录。 |
| 可选模型增强 | OpenAI / Anthropic 兼容 HTTP API | `core/agent_api.py` 为各 Agent 提供可选 JSON 建议接口。 |

---

## 二、多智能体架构

### 2.1 智能体全景图

CTestAgent 的核心是一个进程内同步消息总线。每个 Agent 继承 `BaseAgent`，在启动时通过 `setup()` 注册自己关心的消息主题，然后通过 `publish()` 向总线发布消息。FastAPI 后端会给消息总线挂载 publish hook，把消息事件同步广播给前端。

```text
                             +----------------------+
                             |      Vue 3 UI        |
                             | upload/start/report  |
                             +----------+-----------+
                                        |
                                HTTP / WebSocket
                                        |
                                        v
                             +----------------------+
                             |   FastAPI Runtime    |
                             | CTestAgent/server.py |
                             +----------+-----------+
                                        |
                                        v
+------------------------------------------------------------------+
|                           MessageBus                             |
| subscribe(topic, handler) / publish(topic, data) / message log    |
+----------+-----------+-----------+-----------+-----------+-------+
           |           |           |           |           |
           v           v           v           v           v
    +-------------+ +---------+ +-----------+ +----------+ +----------+
    | Coordinator | |Analyzer | | Generator | | Executor | | Reviewer |
    +------+------+ +----+----+ +-----+-----+ +----+-----+ +----+-----+
           |             |            |            |            |
           +-------------+------------+------------+------------+
                                      |
                                      v
                               +----------+
                               | Reporter |
                               +----------+
```

系统主链路如下：

```text
Coordinator
  -> cmd:start_analysis
Analyzer
  -> data:analysis_result
Coordinator
  -> cmd:generate
Generator
  -> data:test_cases
Reviewer
  -> data:approved_cases
Executor
  -> data:execution_results
Reviewer / Coordinator
  -> feedback:review
Coordinator
  -> cmd:finalize
Reporter
  -> data:report_ready
```

### 2.2 各智能体详细设计

#### 2.2.1 Coordinator 协调者

**角色定位**：Coordinator 是系统总调度者，负责启动分析、推进测试轮次、切换阶段、处理终止条件，并在 Plan A 中统一调度 Reviewer 的跨轮反馈。

**核心职责**：

1. 启动测试流程，发布 `cmd:start_analysis`。
2. 接收 `data:analysis_result`，确定重点函数并切换到 EXPLORE / DEEP 阶段。
3. 每轮发布 `cmd:generate`，指定策略权重、重点函数和批量大小。
4. 接收执行结果和确认崩溃，更新 `GlobalTestState`。
5. 在轮次边界收集 Reviewer 反馈，并在下一轮开始发布。
6. 触发 `cmd:finalize`，要求 Reporter 生成报告。

**关键算法 / 接口**：

```python
def run_testing_loop(self):
    self.global_state.phase = Phase.INIT
    self.publish("cmd:start_analysis", {})
    if not self._analysis_ready:
        self.log_error("分析未完成，无法启动测试循环")
        return
    self._run_rounds(self._focus_functions)
```

```python
def _run_rounds(self, focus_functions: list[str]):
    while not self.global_state.should_stop():
        self.global_state.start_new_round()
        round_num = self.global_state.round_number
        self._emit_deferred_review_feedback(round_num)

        weights = self.explore_weights.copy()
        if self.global_state.phase == Phase.DEEP:
            weights = self.deep_weights.copy()

        cmd = GenerateCommand(
            strategy_weights=weights,
            focus_functions=list(focus_functions),
            batch_size=self._resolve_round_batch_size(False),
            phase=self.global_state.phase,
        )
        self.publish("cmd:generate", cmd)

        summary = self.global_state.finish_round()
        self._collect_review_feedback_for_next_round(round_num)
```

**输入输出数据结构**：

| 输入 | 来源 | 用途 |
| --- | --- | --- |
| `AnalysisResult` | Analyzer | 设置源码总行数、重点函数和阶段起点。 |
| `ExecutionResult[]` | Executor | 记录执行数量、覆盖率和执行状态。 |
| `ConfirmedCrash[]` | Reviewer | 记录已确认缺陷。 |
| `data:executor_error` | Executor | 处理编译失败等致命错误。 |

| 输出 | 去向 | 用途 |
| --- | --- | --- |
| `cmd:start_analysis` | Analyzer | 启动静态分析。 |
| `cmd:generate` | Generator | 驱动每轮用例生成。 |
| `cmd:phase_change` | Generator / 前端事件流 | 通知阶段变化和策略权重变化。 |
| `feedback:review` | Generator | 在下一轮释放 Reviewer 覆盖率反馈。 |
| `cmd:finalize` | Reporter | 触发最终报告生成。 |

#### 2.2.2 Analyzer 分析者

**角色定位**：Analyzer 是测试前的“情报收集者”，负责读取 C 源文件和头文件，提取函数、风险评分、调用关系、参数约束和静态风险点。

**核心职责**：

1. 扫描目标目录中的 `.c` / `.h` 文件。
2. 从头文件声明和 `.c` 函数定义中提取函数签名。
3. 计算函数风险评分：指针参数、圈复杂度、代码行数、危险函数调用。
4. 识别危险 API 和简单静态漏洞模式，例如 `gets`、`strcpy`、`sprintf`、格式化字符串风险、除零风险等。
5. 构建函数调用关系和参数值域提示，供 Generator 选择输入策略。

**关键算法 / 接口**：

```python
def analyze(self) -> AnalysisResult:
    result = AnalysisResult()
    for f in source_dir.glob("*.h"):
        result.header_files.append(str(f))
    for f in source_dir.glob("*.c"):
        result.source_files.append(str(f))

    for header in result.header_files:
        result.functions.extend(self._parse_header(header))

    for src in result.source_files:
        result.functions.extend(self._parse_source_definitions(src))

    result.static_findings = self._scan_static_findings(source_content)
    result.high_risk_functions = [
        name for name, score in result.risk_scores.items()
        if score >= 0.5
    ]
    return result
```

```python
def _compute_risk_score(self, func: FunctionInfo) -> float:
    ptr_ratio = func.pointer_param_count / max(func.param_count, 1)
    complexity_norm = min(func.cyclomatic_complexity / 20.0, 1.0)
    loc_norm = min(func.lines_of_code / 200.0, 1.0)
    score = ptr_ratio * 0.3 + complexity_norm * 0.3 + loc_norm * 0.2
    if func.has_unsafe_calls:
        score += 0.2
    return min(score, 1.0)
```

**输入输出数据结构**：

| 输入 | 来源 | 用途 |
| --- | --- | --- |
| `cmd:start_analysis` | Coordinator | 启动源码分析。 |
| `source_dir` | `config.yaml` / 前端上传 | 指定被测 C 项目目录。 |

| 输出字段 | 类型 | 说明 |
| --- | --- | --- |
| `functions` | `list[FunctionSignature]` | 可识别的函数签名。 |
| `high_risk_functions` | `list[str]` | 风险评分较高的函数。 |
| `risk_scores` | `dict[str, float]` | 每个函数的风险分。 |
| `call_graph` | `dict[str, list[str]]` | 函数调用关系。 |
| `param_constraints` | `dict[str, list[ValueRange]]` | 参数约束和边界值建议。 |
| `static_findings` | `list[dict]` | 静态扫描发现的风险点。 |

#### 2.2.3 Generator 生成者

**角色定位**：Generator 是测试输入的主要生产者，负责根据策略权重、重点函数和 Reviewer 反馈生成测试用例。

**核心职责**：

1. 维护多种生成策略：等价类、边界值、决策表、状态序列、随机错误猜测、白盒引导变异。
2. 接收 `GenerateCommand`，按权重分配每种策略的生成数量。
3. 从目标目录预加载种子文件，并维护有价值输入的种子库。
4. 对表达式解析、二进制文件输入等常见 C 程序类型注入启发式种子。
5. 接收 `feedback:review`，对低多样性、覆盖率缺口、决策表缺口等情况调整生成方向。

**关键算法 / 接口**：

```python
def on_generate_command(self, msg: Message):
    cmd = msg.data
    self.strategy_weights = cmd.strategy_weights or self.strategy_weights
    self.focus_functions = cmd.focus_functions or self.focus_functions
    batch_size = cmd.batch_size or self.batch_size

    heuristic_cases = self._generate_generic_c_seed_cases(batch_size, cmd.phase)
    profile_cases = self._generate_profile_cases(batch_size, cmd.phase)
    strategy_cases = self._generate_batch(batch_size)
    test_cases = self._deduplicate_cases(
        heuristic_cases + profile_cases + strategy_cases,
        batch_size,
    )
    self.publish("data:test_cases", test_cases)
```

```python
def on_reviewer_feedback(self, msg: Message):
    feedback = msg.data
    if feedback.type == FeedbackType.COVERAGE_GAP:
        gap_cases = self.strategies["whitebox_guided"].generate(
            count=20,
            target_function=self.focus_functions[0],
            target_lines=feedback.uncovered_lines,
        )
        self.publish("data:test_cases", gap_cases)
    elif feedback.type == FeedbackType.LOW_DIVERSITY:
        self.strategy_weights["random_error_guess"] += 0.15
        self._normalize_weights()
```

**输入输出数据结构**：

| 输入 | 来源 | 用途 |
| --- | --- | --- |
| `GenerateCommand` | Coordinator | 指定策略权重、重点函数、批量大小和阶段。 |
| `AnalysisResult` | Analyzer | 更新重点函数和约束信息。 |
| `ReviewFeedback` | Reviewer / Coordinator | 按反馈调整生成策略。 |
| `coverage_update` | Executor | 将有新覆盖的用例加入种子库。 |

| 输出 | 去向 | 用途 |
| --- | --- | --- |
| `TestCase[]` | Reviewer | 待审查测试用例。 |

#### 2.2.4 Executor 执行者

**角色定位**：Executor 是测试“试验场”，负责把测试用例真正送入 C 程序执行，并收集运行结果和覆盖率。

**核心职责**：

1. 识别可用 `gcc` / `gcov`，在 Windows 下优先使用 MinGW。
2. 生成目标程序的 harness，或在必要时使用目标自带 `main`。
3. 编译被测 `.c` 文件与 harness，加入 `--coverage` 等插桩参数。
4. 将每个 `TestCase` 写入临时输入文件，调用子进程执行。
5. 捕获 PASS、CRASH、TIMEOUT、ERROR，并记录返回码、stderr、信号和 ASan 类型。
6. 批量收集 `.gcov` 覆盖率快照，发布 `data:coverage_update`。

**关键算法 / 接口**：

```python
def _compile(self):
    c_sources = list(Path(self.source_dir).glob("*.c"))
    self._clean_work_dir()
    harness_code = self._generate_harness()
    compile_cmd = [self.gcc_path, *source_files, "-o", self.harness_path]
    compile_cmd.extend(["-I", str(source_dir), "-Wall", "-g", "-lm"])
    if self.enable_gcov:
        compile_cmd.extend(["--coverage", "-fprofile-arcs", "-ftest-coverage"])
    subprocess.run(compile_cmd, cwd=self.work_dir, timeout=60)
```

```python
def _execute_single(self, case: TestCase) -> ExecutionResult:
    input_file = os.path.join(self.work_dir, f"input_{case.id}.dat")
    with open(input_file, "wb") as f:
        f.write(case.input_data or case.input_text.encode("utf-8"))

    proc = subprocess.run(
        [self.harness_path, input_file],
        capture_output=True,
        timeout=self.timeout,
        cwd=self.work_dir,
    )
    result.return_code = proc.returncode
    result.stderr = proc.stderr.decode("utf-8", errors="replace")
```

**输入输出数据结构**：

| 输入 | 来源 | 用途 |
| --- | --- | --- |
| `AnalysisResult` | Analyzer | 选择通用入口函数并编译目标程序。 |
| `TestCase[]` | Reviewer | 执行通过审查的测试用例。 |

| 输出 | 去向 | 用途 |
| --- | --- | --- |
| `ExecutionResult[]` | Coordinator / Reviewer / Reporter | 执行状态、覆盖率、崩溃信息。 |
| `coverage_update` | Generator / Reviewer | 将新增覆盖样例反馈给生成和评审模块。 |
| `executor_error` | Coordinator / 前端 | 编译失败等致命错误。 |

#### 2.2.5 Reviewer 评审者 ⭐

**角色定位**：Reviewer 是本系统最重要的协作智能体，承担“质检官”和“策略顾问”双重角色。它是 CTestAgent 相比单体测试流水线最明显的差异点。

**核心职责**：

1. 在执行前对 `TestCase` 做去重、有效性、覆盖潜力和多样性评分。
2. 将通过审查的用例发布给 Executor，拒绝明显重复或低价值输入。
3. 对 Executor 返回的崩溃进行签名聚合、严重度评估和风险描述。
4. 维护覆盖率数据，分析未覆盖行和覆盖率增长停滞。
5. 生成 `ReviewFeedback`，由 Coordinator 在下一轮安全释放给 Generator。

**关键算法 / 接口：用例质量审查**

```python
def review_test_cases(self, cases: list[TestCase]) -> ReviewResult:
    for case in cases:
        if self._is_duplicate(case):
            rejected.append((case, "DUPLICATE"))
            continue

        validity = self._assess_validity(case)
        estimated_coverage = self._estimate_coverage(case)
        diversity = self._compute_diversity(case, approved)
        total_score = (
            validity * self.validity_weight +
            estimated_coverage * self.coverage_weight +
            diversity * self.diversity_weight
        )

        if total_score >= self.approval_threshold:
            approved.append(case)
```

**关键算法 / 接口：崩溃确认与聚合**

当前版本没有真正对每个崩溃重复重跑 3 次，而是采用更轻量的“崩溃签名 + 严重度”确认方式。`crash_verify_count` 保留为配置项，后续可以扩展为真正的多次复现。

```python
def verify_crashes(self, crashes: list[ExecutionResult]) -> VerifyResult:
    grouped: dict[str, list[ExecutionResult]] = {}
    for crash in crashes:
        stack_hash = self._build_crash_signature(crash)
        grouped.setdefault(stack_hash, []).append(crash)

    for stack_hash, crash_group in grouped.items():
        representative = crash_group[0]
        if stack_hash in self.known_crash_hashes:
            continue

        severity = self._assess_severity(representative)
        if severity > 0:
            self.known_crash_hashes.add(stack_hash)
            confirmed.append(ConfirmedCrash(
                crash=representative,
                reproducibility=1.0,
                stack_hash=stack_hash,
                occurrence_count=len(crash_group),
            ))
```

**关键算法 / 接口：覆盖率缺口反馈**

```python
def collect_pending_feedback(self) -> list[ReviewFeedback]:
    feedbacks = self.analyze_coverage_gaps()
    self.log_info(
        f"[OBS][feedback_emit] count={len(feedbacks)}, "
        f"types={[fb.type.value for fb in feedbacks]}"
    )
    return feedbacks
```

```python
def analyze_coverage_gaps(self) -> list[ReviewFeedback]:
    growth_rate = self.coverage_data.recent_growth_rate(last_n=200)
    uncovered = self.coverage_data.get_uncovered_lines()
    if len(self.coverage_data.coverage_history) > 50 and growth_rate < 0.001:
        feedbacks.append(ReviewFeedback(type=FeedbackType.STAGNATION))
    if len(uncovered) > 10:
        feedbacks.append(ReviewFeedback(
            type=FeedbackType.COVERAGE_GAP,
            uncovered_lines=uncovered[:100],
        ))
    return feedbacks
```

**输入输出数据结构**：

| 输入 | 来源 | 用途 |
| --- | --- | --- |
| `TestCase[]` | Generator | 预筛选测试用例。 |
| `ExecutionResult[]` | Executor | 聚合崩溃、更新覆盖率。 |
| `coverage_update` | Executor | 观察新增覆盖种子。 |

| 输出 | 去向 | 用途 |
| --- | --- | --- |
| `approved_cases` | Executor | 通过审查的用例。 |
| `feedback:review` | Generator | 低多样性等即时反馈。 |
| `verified_crashes` | Coordinator / Reporter | 去重后的确认崩溃。 |
| `verify_stats` | Reporter | 崩溃验证统计。 |
| `collect_pending_feedback()` | Coordinator 直接调用 | 轮次边界安全收集覆盖率反馈。 |

#### 2.2.6 Reporter 报告者

**角色定位**：Reporter 是系统输出端，负责把执行数据、覆盖率、缺陷、静态发现和消息总线统计汇总为人可读报告。

**核心职责**：

1. 收集 `AnalysisResult` 中的源码行数、源码文件和静态风险点。
2. 收集 `ExecutionResult` 中的执行统计和覆盖率集合。
3. 收集 `ConfirmedCrash`，按崩溃类型、源码位置和栈指纹去重。
4. 将静态发现并入缺陷列表，避免没有动态崩溃时报告为空。
5. 生成 HTML 报告和 JSON 报告。
6. 统计消息总线交互，用于前端“智能体日志”和“统计图表”展示。

**关键算法 / 接口**：

```python
def on_finalize(self, msg: Message):
    self.log_info("开始生成测试报告...")
    report = self._build_report()
    report_path = self._generate_html_report(report)
    json_path = self._generate_json_report(report)
    self.publish("data:report_ready", report_path)
```

```python
def _build_report(self) -> TestReport:
    total_pass = sum(1 for r in self.all_execution_results if r.status == TestCaseStatus.PASS)
    total_crash = sum(1 for r in self.all_execution_results if r.status == TestCaseStatus.CRASH)
    interaction_stats = self.bus.get_interaction_stats()
    return TestReport(
        project_name=self.project_name,
        total_cases_executed=len(self.all_execution_results),
        total_cases_passed=total_pass,
        total_crashes=total_crash,
        confirmed_bugs=bug_reports,
        line_coverage=line_coverage,
        branch_coverage=branch_coverage,
        agent_interaction_stats=interaction_stats,
    )
```

**输入输出数据结构**：

| 输入 | 来源 | 用途 |
| --- | --- | --- |
| `AnalysisResult` | Analyzer | 源码文件、总行数、静态发现。 |
| `ExecutionResult[]` | Executor | 执行统计和覆盖率。 |
| `ConfirmedCrash[]` | Reviewer | 确认缺陷。 |
| `verify_stats` | Reviewer | 误报与确认统计。 |
| `FinalizeCommand` | Coordinator | 触发报告生成。 |

| 输出 | 去向 | 用途 |
| --- | --- | --- |
| `report_*.html` | 文件系统 | 人可读测试报告。 |
| `report_*.json` | 文件系统 / 前端 | 前端底部面板数据源。 |
| `data:report_ready` | Coordinator / server hook | 标记本次测试完成。 |

---

## 三、消息总线与通信协议

### 3.1 消息总线设计

`MessageBus` 是 CTestAgent 的通信基础设施，采用进程内同步发布 / 订阅模型。同步模型的优点是实现简单、调试直观、无需引入外部队列；缺点是消息处理函数会在同一调用栈中连续执行，因此高频反馈需要特别设计，避免递归触发。

```python
class MessageBus:
    def __init__(self):
        self._subscribers = defaultdict(list)
        self._message_log = []
        self._publish_hooks = []
        self._agents = {}

    def subscribe(self, topic: str, handler: Callable):
        self._subscribers[topic].append(handler)

    def publish(self, topic: str, data: Any, sender: str = ""):
        msg = Message(topic=topic, data=data, sender=sender)
        self._message_log.append(msg)
        for hook in list(self._publish_hooks):
            hook(msg)
        for handler in self._subscribers.get(topic, []):
            handler(msg)
```

消息总线同时承担三项工程职责：

1. 解耦 Agent：发布者不需要知道订阅者是谁。
2. 留存协作日志：Reporter 调用 `get_interaction_stats()` 统计消息数量。
3. 连接前端：`server.py` 给总线注册 publish hook，将每条消息转为 WebSocket 事件。

### 3.2 消息主题与订阅关系

| 消息主题 | 主要发布者 | 当前订阅者 / 消费者 | 说明 |
| --- | --- | --- | --- |
| `cmd:start_analysis` | Coordinator | Analyzer | 启动源码分析。 |
| `data:analysis_result` | Analyzer | Coordinator、Generator、Executor、Reporter | 共享函数、风险、源码行数和静态发现。 |
| `cmd:generate` | Coordinator | Generator | 启动一轮测试用例生成。 |
| `data:test_cases` | Generator | Reviewer | 待审查测试用例。 |
| `feedback:review` | Reviewer、Coordinator | Generator | Reviewer 反馈，部分即时发布，覆盖率缺口由 Coordinator 跨轮发布。 |
| `data:approved_cases` | Reviewer | Executor | 通过质量审查的测试用例。 |
| `data:execution_results` | Executor | Coordinator、Reviewer、Reporter | 执行状态、覆盖率和异常信息。 |
| `data:coverage_update` | Executor | Generator、Reviewer | 有新增覆盖的测试用例，用于种子更新和观察。 |
| `data:verified_crashes` | Reviewer | Coordinator、Reporter | 聚合后的确认崩溃。 |
| `data:verify_stats` | Reviewer | Reporter | 确认 / 误报统计。 |
| `data:executor_error` | Executor | Coordinator | 编译失败等致命执行错误。 |
| `cmd:phase_change` | Coordinator | Generator | 阶段切换通知和新权重。 |
| `cmd:finalize` | Coordinator | Reporter | 触发最终报告生成。 |
| `data:report_ready` | Reporter | Coordinator、server hook | 报告生成完成，前端可拉取报告。 |
| `feedback:strategy` | 预留接口 | Coordinator | 当前代码保留订阅，用于后续策略反馈扩展。 |

### 3.3 单轮协作时序图

```text
Coordinator        Generator          Reviewer           Executor           Reporter
    |                  |                  |                  |                  |
    | cmd:generate     |                  |                  |                  |
    |----------------->|                  |                  |                  |
    |                  | data:test_cases  |                  |                  |
    |                  |----------------->|                  |                  |
    |                  |                  | review cases      |                  |
    |                  |                  | data:approved     |                  |
    |                  |                  |----------------->|                  |
    |                  |                  |                  | compile/run      |
    |                  |                  |                  | collect gcov     |
    |                  |                  | data:execution_results             |
    |<------------------------------------------------------|----------------->|
    |                  |                  | verify crashes    |                  |
    |                  |                  | update coverage   |                  |
    |                  |                  | data:verified_crashes              |
    |<-----------------------------------|----------------------------------->|
    | finish_round      |                  |                  |                  |
    | collect_pending_feedback()          |                  |                  |
    |------------------------------------>|                  |                  |
    | cache feedback for next round       |                  |                  |
```

---

## 四、对抗与协作机制

### 4.1 Generator vs Reviewer 的对抗博弈

Generator 与 Reviewer 之间形成“生成 - 审查 - 反馈”的对抗关系。

```text
        Generator                                  Reviewer
   +----------------+                        +----------------+
   |  生成测试用例   | ---- data:test_cases -> |  质量审查       |
   |  调整策略权重   | <- feedback:review ---- |  缺口分析       |
   |  维护种子库     | <- coverage_update ---- |  崩溃确认       |
   +----------------+                        +----------------+
```

Generator 的目标是提高覆盖率、触发崩溃、探索更多路径；Reviewer 的目标是减少低价值输入、压缩重复样例、识别覆盖率缺口。二者的关系并不是“谁替代谁”，而是形成测试效率的正反馈：

- 当用例通过率过低时，Reviewer 发出 `LOW_DIVERSITY`，Generator 增加随机错误猜测或变异力度。
- 当存在大量未覆盖行时，Reviewer 生成 `COVERAGE_GAP`，Generator 调用白盒引导策略补洞。
- 当执行产生新覆盖时，Executor 发出 `coverage_update`，Generator 将相应用例加入种子库。
- 当崩溃重复出现时，Reviewer 通过签名聚合，避免报告被同一缺陷刷屏。

### 4.2 量化评估指标：RoundMetrics

`RoundMetrics` 用于衡量单轮协作效率。当前代码在 `GlobalTestState.finish_round()` 中计算这些指标，作为后续报告分析和扩展策略评估的基础。

```python
@dataclass
class RoundMetrics:
    total_generated: int = 0
    reviewer_approved: int = 0
    approval_rate: float = 0.0
    new_coverage_lines: int = 0
    new_crashes_found: int = 0
    false_positives_caught: int = 0
    coverage_per_case: float = 0.0
    useful_case_ratio: float = 0.0
    feedback_hit_rate: float = 0.0
```

| 指标 | 含义 | 用途 |
| --- | --- | --- |
| `total_generated` | 本轮生成的测试用例数。 | 衡量生成规模。 |
| `reviewer_approved` | Reviewer 通过的用例数。 | 衡量审查通过量。 |
| `approval_rate` | 通过率。 | 判断 Reviewer 是否过严或生成器是否低效。 |
| `new_coverage_lines` | 新覆盖代码行数。 | 衡量覆盖率贡献。 |
| `new_crashes_found` | 本轮新确认崩溃数。 | 衡量缺陷发现能力。 |
| `coverage_per_case` | 每个执行用例带来的平均新覆盖。 | 衡量执行效率。 |
| `useful_case_ratio` | 新覆盖和新缺陷相对执行量的比例。 | 衡量输入价值。 |

---

## 五、关键工程问题与解决方案 ⭐

### 5.1 同步消息总线的反馈递归问题：B1 失败案例

同步消息总线的特点是：`publish()` 不只是把消息放进队列，而是立即调用所有订阅者。这个模型使系统容易调试，但也带来一个风险：如果某个订阅者在处理消息时继续发布会触发上游生成的消息，就会出现嵌套调用。

B1 问题可以抽象为以下调用链：

```text
Executor.publish(data:execution_results)
  -> Reviewer.on_execution_results()
      -> Reviewer.publish(feedback:review)
          -> Generator.on_reviewer_feedback()
              -> Generator.publish(data:test_cases)
                  -> Reviewer.on_test_cases()
                      -> Reviewer.publish(data:approved_cases)
                          -> Executor.on_approved_cases()
                              -> Executor.publish(data:execution_results)
```

这种结构的问题不在于反馈本身，而在于反馈发生在 `data:execution_results` 的同步调用栈内部。结果是：

1. 轮次边界被打乱，Coordinator 还没完成当前轮汇总，下一批用例已经开始执行。
2. Reporter 和 Coordinator 对同一批执行结果的观察顺序可能变得难以推理。
3. 覆盖率反馈容易形成反馈风暴，难以进行严格对照实验。
4. 日志中的 feedback_emit 与实际轮次对应关系不清晰。

因此，B1 的失败经验说明：在同步消息总线中，反馈必须受到 Coordinator 的轮次边界约束。

### 5.2 跨轮异步反馈调度：Plan A 设计

Plan A 的解决思路是“不取消反馈，而是移动反馈发生的位置”。Reviewer 不在 `on_execution_results()` 内直接发布覆盖率缺口反馈，而是只更新内部状态；当 Coordinator 完成本轮执行和汇总后，主动调用 Reviewer 的 `collect_pending_feedback()` 收集反馈，并缓存到下一轮开头释放。

核心设计如下：

```python
def _collect_review_feedback_for_next_round(self, round_num: int):
    reviewer = self._get_reviewer_agent()
    collector = getattr(reviewer, "collect_pending_feedback", None)
    feedbacks = collector()
    self._deferred_review_feedback = [
        fb for fb in feedbacks
        if isinstance(fb, ReviewFeedback)
    ]
```

```python
def _emit_deferred_review_feedback(self, round_num: int):
    feedbacks = list(self._deferred_review_feedback)
    self._deferred_review_feedback = []
    for feedback in feedbacks:
        self.publish("feedback:review", feedback)
```

Plan A 的实际时序如下：

```text
第 N 轮：
Executor -> data:execution_results
Reviewer -> 更新覆盖率状态，但不直接发布覆盖率反馈
Coordinator -> finish_round()
Coordinator -> collect_pending_feedback()
Coordinator -> 缓存 feedback

第 N+1 轮：
Coordinator -> emit_deferred_feedback()
Generator -> 根据 feedback 生成定向用例
Coordinator -> cmd:generate
```

这个方案的价值是：

- 保留 Reviewer 的反馈能力。
- 避免同步消息总线的递归调用。
- 让 feedback_emit 与轮次边界对应，便于实验统计。
- 让 Coordinator 成为反馈节奏的唯一仲裁者。

### 5.3 严格对照实验方法

为了证明 Plan A 不是“日志变多”而是真正让协作机制生效，实验采用严格对照方式：

| 控制项 | 说明 |
| --- | --- |
| 被测目标 | 使用同一组 C 测试目标和相同输入场景。 |
| 执行环境 | 使用同一后端、同一编译器和相同运行参数。 |
| 比较版本 | master 基线与 Plan A 协作版本。 |
| 核心指标 | 最终覆盖率、缺陷发现数、feedback_emit 次数。 |
| 数据来源 | `output/final_comparison/master_baseline.log` 与 `output/final_comparison/plan_a_with_feedback.log`。 |

本项目没有使用未在实验中出现的额外指标来包装结果，而是保留三个最直接指标：覆盖率、缺陷数和反馈触发次数。

---

## 六、实验设计与结果

### 6.1 实验环境

| 项目 | 配置 |
| --- | --- |
| 操作系统 | Windows 环境，项目中包含 MinGW 路径自动识别逻辑。 |
| Python | Python 3.10+。 |
| 编译器 | GCC / MinGW GCC。 |
| 覆盖率工具 | gcov。 |
| 前端 | Vue 3 + Vite。 |
| 后端 | FastAPI + WebSocket + 进程内 MessageBus。 |

### 6.2 对照实验设计

实验比较两种配置：

| 配置 | 说明 |
| --- | --- |
| 单体探索基线 | 主要观察基础生成、执行、报告链路，Reviewer 覆盖率反馈触发较少。 |
| 多智能体协作 | 启用 Plan A 跨轮反馈调度，让 Reviewer 覆盖率反馈在轮次边界被 Coordinator 统一释放。 |

两组实验关注相同指标：

1. 最终覆盖率是否下降。
2. 缺陷发现数是否保持。
3. Reviewer 反馈是否真正被触发并进入协作链路。

### 6.3 实验结果

| 配置 | 最终覆盖率 | 反馈触发次数 | 缺陷发现数 |
| --- | --- | ---: | ---: |
| 单体探索基线 | 45.38% | 2 | 2 |
| 多智能体协作 | 45.38% | 31 | 2 |

### 6.4 结果分析

实验结果显示，Plan A 多智能体协作版本在最终覆盖率和缺陷发现数上保持不变，说明跨轮反馈调度没有破坏原有测试能力；同时 feedback_emit 从 2 次提升到 31 次，说明 Reviewer 的覆盖率缺口分析更频繁地进入协作流程。

该结果的意义不是“覆盖率一定更高”，而是证明了多智能体系统的关键机制已经实际运行：Reviewer 能持续观察测试状态，Coordinator 能安全调度反馈，Generator 能接收反馈并生成后续用例。对于课程设计项目而言，这比单纯展示一个最终覆盖率数字更能体现协作式测试系统的工程价值。

---

## 七、创新点总结

1. **多智能体职责拆分**：将 C 语言自动化测试拆分为分析、生成、执行、评审、调度、报告 6 个独立角色。
2. **Reviewer 评审智能体**：在执行前审查用例质量，在执行后聚合崩溃并分析覆盖率缺口。
3. **同步消息总线可观测化**：所有 Agent 通信通过 `MessageBus` 记录，可被 Reporter 和前端实时展示。
4. **Plan A 跨轮反馈机制**：通过 Coordinator 在轮次边界收集和释放反馈，解决同步总线递归问题。
5. **前端可操作测试闭环**：用户上传 C 文件后即可启动测试，并在页面查看覆盖率、缺陷、日志和图表。
6. **动态与静态结果结合**：Reporter 同时汇总运行时崩溃和 Analyzer 静态风险发现，使报告内容更完整。

---

## 八、已知限制与未来工作

### 8.1 已知限制

| 限制 | 说明 |
| --- | --- |
| Harness 泛化能力有限 | 对复杂结构体、回调、多入口和外部依赖较强的 C 程序，自动 harness 仍可能不足。 |
| ASan 与 gcov 存在冲突风险 | 程序崩溃后可能导致 gcov 数据未写回，影响覆盖率统计完整性。 |
| Windows 默认关闭 ASan | 当前代码在 Windows 下默认关闭 ASan，崩溃判断主要依赖返回码、信号和 stderr。 |
| Reviewer 复现验证仍是简化版 | 当前崩溃确认以签名聚合和严重度为主，未真正对每个崩溃重复执行多次。 |
| 消息总线是同步模型 | 实现简单但不适合高并发、多进程或分布式 Agent 场景。 |
| 通用 C 输入理解有限 | 系统通过启发式识别表达式、二进制文件等常见输入形态，但还不能理解所有业务协议。 |

### 8.2 未来工作

1. 增强 harness 生成能力，支持多入口函数、结构体构造、回调和文件依赖。
2. 将 `crash_verify_count` 落地为真正的多次崩溃复现与敏感性测试。
3. 引入更精确的静态分析或轻量 AST 解析，减少正则解析 C 代码的误判。
4. 将同步 MessageBus 演进为可配置的异步队列，提升高频反馈稳定性。
5. 扩展报告中的 RoundMetrics 可视化，显示每轮通过率、覆盖率增量和反馈命中效果。
6. 增强前端参数与后端策略之间的映射说明，便于用户理解每个滑块的测试含义。

